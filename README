MiMiProxy (experimental)
========================
Small SOCKS5 proxy server on python3.

### use:
```shell
proxy.py 0.0.0.0 1080
```

### Roadmap
* setup.py
* domain name support for cmd connect
* CMD BIND
* CMD UDP ASSOCIATE
* AUTH
